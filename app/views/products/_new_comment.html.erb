<% if signed_in? %>
	<h1 class="center sm-heading">Add a review:</h1>
	<%= form_for([@product, @product.comments.build]) do |f| %>
	<p>
		<div class="form-group">
			<!--<%= f.label :body, "Comments" %><br>-->
			<%= f.text_area :body, :class => "form-control", :placeholder =>"Enter your comments", autofocus: true %>
		
	</p>
	
	<p>
		<div class="rating"></div>"Rating required"
	</p>
	
	<p>
		<%= f.submit "Submit", :class => "btn btn-default mbottom200"  %>
		</div>
	</p>
		</div>
	<% end %>
<% end %>